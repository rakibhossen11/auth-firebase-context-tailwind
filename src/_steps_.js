/*
Context api
1. create context (export)
2. create provider
3. set context value
4. to access the value: useContext hook
*/ 

/**
 * 1. main,jsx set AuthProvidor 
 * 2. inside providor access children props and use 
 * */ 

/**
 * 1. break down the create user function
 * 2. auth providor will have auth and pass email and passwors from register 
 * **/ 